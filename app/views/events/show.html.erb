<div class="container my-5">
<h3>予約内容の確認</h3>

<p><strong>観戦試合:</strong> <%= @event.match %></p>
<p><strong>観戦日:</strong> <%= @event.date %></p>
<p><strong>開始時間:</strong> <%= @event.start_time %></p>
<p><strong>観戦場所:</strong> <%= @event.location %></p>

<div class="col-md-6 col-md-offset-3">
<%= form_with(model: @event_listing,local: true) do |f| %>
  <%= f.hidden_field :event_id, value: @event.id %>
  
  <%= f.label :capacity,"募集人数",class: "form-label" %>
  <%= f.text_field :capacity,class: "form-control"  %>

  <%= f.label :deadline,"募集締切",class: "form-label" %>
  <%= f.date_field :deadline,class: "form-control"  %>

  <%= f.label :message,"メッセージ",class: "form-label" %>
  <%= f.text_area :message,class: "form-control"  %>

  <%= f.submit "募集", class: "btn btn-primary my-2 " %>
<% end %>
</div>
</div>